// massive-sprites - Plots and moves a lot of sprite images and polylines onto WebGL 2.5D surface.
// Copyright (c) Kouji Matsui. (@kekyo@mi.kekyo.net)
// Under MIT.
// https://github.com/kekyo/massive-sprites

// This file is auto-generated by scripts/generate-layout.mjs.
// Do not edit this file directly.

#pragma once

namespace {

{{
set schema (require './schema.cjs').command
set bufferHeaderFields schema.bufferHeader
set commandHeaderFields schema.commandHeader
set resultHeaderFields schema.resultHeader
set optionalFields schema.structs.optional
set valueFields schema.structs.value
set rotationFields schema.structs.rotation
set elementFields schema.elementFields
set polylineNodeFields schema.polylineNodeFields
set polylineColorFields schema.polylineColorFields
set addSpriteBaseFields schema.addSpriteBaseFields
set addPolylineBaseFields schema.addPolylineBaseFields
set updateSpriteBaseFields schema.updateSpriteBaseFields
set updatePolylineBaseFields schema.updatePolylineBaseFields
set updateCameraFields schema.commands.updateCamera
set adjustCameraPositionFields schema.commands.adjustCameraPosition
set setTextureInfoFields schema.commands.setTextureInfo
set setTiledTextureInfoFields schema.commands.setTiledTextureInfo
set setTextureTileInfoFields schema.commands.setTextureTileInfo
set updateElementFieldsCount (add 2 (length elementFields))
set headerSize (length commandHeaderFields)
set addSpriteBaseOffset (add headerSize (length addSpriteBaseFields))
set updateSpriteBaseOffset (add headerSize (length updateSpriteBaseFields))
set addPolylineBaseOffset (add headerSize (length addPolylineBaseFields))
set updatePolylineBaseOffset (add headerSize (length updatePolylineBaseFields))
}}

constexpr int COMMAND_BUFFER_HEADER_FIELDS = {{length bufferHeaderFields}};
{{for i (range 0 (length bufferHeaderFields))}}constexpr int COMMAND_BUFFER_{{at i bufferHeaderFields}}_OFFSET = {{i}};
{{end}}

constexpr int COMMAND_HEADER_FIELDS = {{length commandHeaderFields}};
{{for i (range 0 (length commandHeaderFields))}}constexpr int COMMAND_HEADER_{{at i commandHeaderFields}}_OFFSET = {{i}};
{{end}}

constexpr int COMMAND_RESULT_HEADER_FIELDS = {{length resultHeaderFields}};
{{for i (range 0 (length resultHeaderFields))}}constexpr int COMMAND_RESULT_{{at i resultHeaderFields}}_OFFSET = {{i}};
{{end}}

{{for entry (Object.entries schema.commandOps)}}constexpr int COMMAND_OP_{{at 0 entry}} = {{at 1 entry}};
{{end}}

{{for entry (Object.entries schema.enums.imageMode)}}constexpr int COMMAND_IMAGE_MODE_{{at 0 entry}} = {{at 1 entry}};
{{end}}

{{for entry (Object.entries schema.enums.borderMode)}}constexpr int COMMAND_BORDER_MODE_{{at 0 entry}} = {{at 1 entry}};
{{end}}

{{for entry (Object.entries schema.enums.polylineCorrectionMode)}}constexpr int COMMAND_POLYLINE_CORRECTION_MODE_{{at 0 entry}} = {{at 1 entry}};
{{end}}

{{for entry (Object.entries schema.enums.elementKind)}}constexpr int COMMAND_ELEMENT_KIND_{{at 0 entry}} = {{at 1 entry}};
{{end}}

{{for entry (Object.entries schema.enums.updateElementKind)}}constexpr int COMMAND_UPDATE_ELEMENT_KIND_{{at 0 entry}} = {{at 1 entry}};
{{end}}

constexpr int COMMAND_OPTIONAL_FIELDS = {{length optionalFields}};
{{for i (range 0 (length optionalFields))}}constexpr int COMMAND_OPTIONAL_{{at i optionalFields}}_OFFSET = {{i}};
{{end}}

constexpr int COMMAND_VALUE_FIELDS = {{length valueFields}};
{{for i (range 0 (length valueFields))}}constexpr int COMMAND_VALUE_{{at i valueFields}}_OFFSET = {{i}};
{{end}}

constexpr int COMMAND_ROTATION_FIELDS = {{length rotationFields}};
{{for i (range 0 (length rotationFields))}}constexpr int COMMAND_ROTATION_{{at i rotationFields}}_OFFSET = {{i}};
{{end}}

constexpr int COMMAND_ELEMENT_FIELDS = {{length elementFields}};
{{for i (range 0 (length elementFields))}}constexpr int COMMAND_ELEMENT_{{at i elementFields}}_OFFSET = {{i}};
{{end}}

constexpr int COMMAND_POLYLINE_NODE_FIELDS = {{length polylineNodeFields}};
{{for i (range 0 (length polylineNodeFields))}}constexpr int COMMAND_POLYLINE_NODE_{{at i polylineNodeFields}}_OFFSET = {{i}};
{{end}}

constexpr int COMMAND_UPDATE_ELEMENT_FIELDS = {{updateElementFieldsCount}};
constexpr int COMMAND_UPDATE_ELEMENT_INDEX_OFFSET = 0;
constexpr int COMMAND_UPDATE_ELEMENT_KIND_OFFSET = 1;
constexpr int COMMAND_UPDATE_ELEMENT_DATA_OFFSET = 2;

constexpr int COMMAND_UPDATE_CAMERA_FIELDS = {{add headerSize (length updateCameraFields)}};
{{for i (range 0 (length updateCameraFields))}}constexpr int COMMAND_UPDATE_CAMERA_{{at i updateCameraFields}}_OFFSET = {{add headerSize i}};
{{end}}

constexpr int COMMAND_ADJUST_CAMERA_POSITION_FIELDS = {{add headerSize (length adjustCameraPositionFields)}};
{{for i (range 0 (length adjustCameraPositionFields))}}constexpr int COMMAND_ADJUST_CAMERA_POSITION_{{at i adjustCameraPositionFields}}_OFFSET = {{add headerSize i}};
{{end}}

constexpr int COMMAND_SET_TEXTURE_INFO_FIELDS = {{add headerSize (length setTextureInfoFields)}};
{{for i (range 0 (length setTextureInfoFields))}}constexpr int COMMAND_SET_TEXTURE_INFO_{{at i setTextureInfoFields}}_OFFSET = {{add headerSize i}};
{{end}}

constexpr int COMMAND_SET_TILED_TEXTURE_INFO_FIELDS = {{add headerSize (length setTiledTextureInfoFields)}};
{{for i (range 0 (length setTiledTextureInfoFields))}}constexpr int COMMAND_SET_TILED_TEXTURE_INFO_{{at i setTiledTextureInfoFields}}_OFFSET = {{add headerSize i}};
{{end}}

constexpr int COMMAND_SET_TEXTURE_TILE_INFO_FIELDS = {{add headerSize (length setTextureTileInfoFields)}};
{{for i (range 0 (length setTextureTileInfoFields))}}constexpr int COMMAND_SET_TEXTURE_TILE_INFO_{{at i setTextureTileInfoFields}}_OFFSET = {{add headerSize i}};
{{end}}

constexpr int COMMAND_ADD_SPRITE_BASE_FIELDS = {{addSpriteBaseOffset}};
constexpr int COMMAND_ADD_SPRITE_ELEMENT_BASE_OFFSET = {{addSpriteBaseOffset}};
{{for i (range 0 (length addSpriteBaseFields))}}constexpr int COMMAND_ADD_SPRITE_{{at i addSpriteBaseFields}}_OFFSET = {{add headerSize i}};
{{end}}

constexpr int COMMAND_ADD_POLYLINE_BASE_FIELDS = {{addPolylineBaseOffset}};
constexpr int COMMAND_ADD_POLYLINE_NODE_BASE_OFFSET = {{addPolylineBaseOffset}};
{{for i (range 0 (length addPolylineBaseFields))}}constexpr int COMMAND_ADD_POLYLINE_{{at i addPolylineBaseFields}}_OFFSET = {{add headerSize i}};
{{end}}

constexpr int COMMAND_UPDATE_SPRITE_BASE_FIELDS = {{updateSpriteBaseOffset}};
constexpr int COMMAND_UPDATE_SPRITE_ELEMENT_BASE_OFFSET = {{updateSpriteBaseOffset}};
{{for i (range 0 (length updateSpriteBaseFields))}}constexpr int COMMAND_UPDATE_SPRITE_{{at i updateSpriteBaseFields}}_OFFSET = {{add headerSize i}};
{{end}}

constexpr int COMMAND_UPDATE_POLYLINE_BASE_FIELDS = {{updatePolylineBaseOffset}};
constexpr int COMMAND_UPDATE_POLYLINE_NODE_BASE_OFFSET = {{updatePolylineBaseOffset}};
{{for i (range 0 (length updatePolylineBaseFields))}}constexpr int COMMAND_UPDATE_POLYLINE_{{at i updatePolylineBaseFields}}_OFFSET = {{add headerSize i}};
{{end}}

constexpr int COMMAND_REMOVE_SPRITE_FIELDS = {{add headerSize 1}};
constexpr int COMMAND_REMOVE_SPRITE_SPRITE_ID_OFFSET = {{headerSize}};

constexpr int COMMAND_REMOVE_POLYLINE_FIELDS = {{add headerSize 1}};
constexpr int COMMAND_REMOVE_POLYLINE_POLYLINE_ID_OFFSET = {{headerSize}};

} // namespace
