// massive-sprites - Plots and moves a lot of sprite images and polylines onto WebGL 2.5D surface.
// Copyright (c) Kouji Matsui. (@kekyo@mi.kekyo.net)
// Under MIT.
// https://github.com/kekyo/massive-sprites

// This file is auto-generated by scripts/generate-layout.mjs.
// Do not edit this file directly.

#pragma once

namespace {

{{set schema (require './schema.cjs').wasm}}
constexpr int WASM_OUTPUT_STRIDE = {{schema.wasmOutputStride}};
constexpr int POLYLINE_OUTPUT_STRIDE = {{schema.polylineOutputStride}};
constexpr int WASM_MAX_ELEMENTS_PER_SPRITE = {{schema.maxElementsPerSprite}};

{{for i (range 0 (length schema.spriteFields))}}constexpr int SPRITE_{{at i schema.spriteFields}}_OFFSET = {{i}};
{{end}}
constexpr int WASM_SPRITE_INPUT_FIELDS = {{length schema.spriteFields}};
constexpr int WASM_SPRITE_INPUT_AOS_STRIDE = WASM_SPRITE_INPUT_FIELDS;

{{for i (range 0 (length schema.elementFields))}}constexpr int SPRITE_ELEMENT_{{at i schema.elementFields}}_OFFSET = {{i}};
{{end}}
constexpr int WASM_ELEMENT_INPUT_FIELDS = {{length schema.elementFields}};
constexpr int WASM_INPUT_FIELDS = WASM_ELEMENT_INPUT_FIELDS;
constexpr int WASM_ELEMENT_INPUT_AOS_STRIDE = WASM_ELEMENT_INPUT_FIELDS;
constexpr int WASM_INPUT_AOS_STRIDE = WASM_ELEMENT_INPUT_AOS_STRIDE;

{{for i (range 0 (length schema.polylineFields))}}constexpr int POLYLINE_{{at i schema.polylineFields}}_OFFSET = {{i}};
{{end}}
constexpr int WASM_POLYLINE_INPUT_FIELDS = {{length schema.polylineFields}};
constexpr int WASM_POLYLINE_INPUT_AOS_STRIDE = WASM_POLYLINE_INPUT_FIELDS;

{{for i (range 0 (length schema.polylineNodeFields))}}constexpr int POLYLINE_NODE_{{at i schema.polylineNodeFields}}_OFFSET = {{i}};
{{end}}
constexpr int WASM_POLYLINE_NODE_INPUT_FIELDS = {{length schema.polylineNodeFields}};
constexpr int WASM_POLYLINE_NODE_INPUT_AOS_STRIDE = WASM_POLYLINE_NODE_INPUT_FIELDS;

{{for i (range 0 (length schema.stateSpriteFields))}}constexpr int STATE_SPRITE_{{at i schema.stateSpriteFields}}_OFFSET = {{i}};
{{end}}
constexpr int WASM_STATE_SPRITE_STRIDE = {{length schema.stateSpriteFields}};

{{for i (range 0 (length schema.stateElementFields))}}constexpr int STATE_ELEMENT_{{at i schema.stateElementFields}}_OFFSET = {{i}};
{{end}}
constexpr int WASM_STATE_ELEMENT_STRIDE = {{length schema.stateElementFields}};

{{for i (range 0 (length schema.statePolylineFields))}}constexpr int STATE_POLYLINE_{{at i schema.statePolylineFields}}_OFFSET = {{i}};
{{end}}
constexpr int WASM_STATE_POLYLINE_STRIDE = {{length schema.statePolylineFields}};

{{for i (range 0 (length schema.cameraFields))}}constexpr int CAMERA_{{at i schema.cameraFields}}_OFFSET = {{i}};
{{end}}
constexpr int CAMERA_BUFFER_SIZE = {{length schema.cameraFields}};

{{for i (range 0 (length schema.applyStatsFields))}}constexpr int APPLY_STATS_{{at i schema.applyStatsFields}}_OFFSET = {{i}};
{{end}}
constexpr int APPLY_STATS_FIELDS = {{length schema.applyStatsFields}};

{{for i (range 0 (length schema.computeStatsFields))}}constexpr int COMPUTE_STATS_{{at i schema.computeStatsFields}}_OFFSET = {{i}};
{{end}}
constexpr int COMPUTE_STATS_FIELDS = {{length schema.computeStatsFields}};

constexpr int DRAW_COMMAND_HEADER_FIELDS = {{length schema.drawCommandHeaderFields}};
{{for i (range 0 (length schema.drawCommandHeaderFields))}}constexpr int DRAW_COMMAND_{{at i schema.drawCommandHeaderFields}}_OFFSET = {{i}};
{{end}}

constexpr int DRAW_COMMAND_FIELDS = {{length schema.drawCommandFields}};
{{for i (range 0 (length schema.drawCommandFields))}}constexpr int DRAW_COMMAND_{{at i schema.drawCommandFields}}_FIELD_OFFSET = {{i}};
{{end}}

{{for entry (Object.entries schema.drawCommandKinds)}}constexpr int DRAW_COMMAND_KIND_{{at 0 entry}} = {{at 1 entry}};
{{end}}

constexpr int INTERPOLATION_MODE_FEEDBACK = {{schema.interpolation.modeFeedback}};
constexpr int INTERPOLATION_MODE_FEEDFORWARD = {{schema.interpolation.modeFeedforward}};
constexpr int INTERPOLATION_EASING_LINEAR = {{schema.interpolation.easingLinear}};
constexpr int INTERPOLATION_EASING_SIGMOID = {{schema.interpolation.easingSigmoid}};
constexpr int INTERPOLATION_EASING_EASE = {{schema.interpolation.easingEase}};
constexpr int INTERPOLATION_EASING_EXPONENTIAL = {{schema.interpolation.easingExponential}};
constexpr int INTERPOLATION_EASING_QUADRATIC = {{schema.interpolation.easingQuadratic}};
constexpr int INTERPOLATION_EASING_CUBIC = {{schema.interpolation.easingCubic}};
constexpr int INTERPOLATION_EASING_SINE = {{schema.interpolation.easingSine}};
constexpr int INTERPOLATION_EASING_BOUNCE = {{schema.interpolation.easingBounce}};
constexpr int INTERPOLATION_EASING_BACK = {{schema.interpolation.easingBack}};

constexpr int COMMON_RENDER_MODE_SURFACE = {{schema.common.renderMode.surface}};
constexpr int COMMON_RENDER_MODE_BILLBOARD = {{schema.common.renderMode.billboard}};
constexpr int COMMON_RENDER_MODE_BILLBOARD_PERSPECTIVE = {{schema.common.renderMode.billboardPerspective}};
constexpr int AUTO_DIRECTION_MODE_NONE = {{schema.common.autoDirectionMode.NONE}};
constexpr int AUTO_DIRECTION_MODE_ROTATION = {{schema.common.autoDirectionMode.ROTATION}};
constexpr int AUTO_DIRECTION_MODE_FLIPPING = {{schema.common.autoDirectionMode.FLIPPING}};
constexpr int AUTO_DIRECTION_SPACE_WORLD = {{schema.common.autoDirectionSpace.WORLD}};
constexpr int AUTO_DIRECTION_SPACE_PARENT_LOCAL = {{schema.common.autoDirectionSpace.PARENT_LOCAL}};
constexpr int POLYLINE_CORRECTION_MODE_NONE = {{schema.common.polylineCorrectionMode.NONE}};
constexpr int POLYLINE_CORRECTION_MODE_FAN = {{schema.common.polylineCorrectionMode.FAN}};
constexpr int CAMERA_TRACKING_TARGET_MODE_BASE = {{schema.common.cameraTracking.targetMode.base}};
constexpr int CAMERA_TRACKING_TARGET_MODE_CONTENT_APPROX = {{schema.common.cameraTracking.targetMode.contentApprox}};
constexpr int CAMERA_TRACKING_INTERPOLATION_KEEP = {{schema.common.cameraTracking.interpolationKind.keep}};
constexpr int CAMERA_TRACKING_INTERPOLATION_CLEAR = {{schema.common.cameraTracking.interpolationKind.clear}};
constexpr int CAMERA_TRACKING_INTERPOLATION_SET = {{schema.common.cameraTracking.interpolationKind.set}};
constexpr double CAMERA_TRACKING_DEFAULT_FIT_PADDING = {{schema.common.cameraTracking.defaults.fitPadding}};
constexpr double CAMERA_TRACKING_DEFAULT_ZOOM_BIAS = {{schema.common.cameraTracking.defaults.fitZoomBias}};
constexpr double COMMON_DEFAULT_CAMERA_FOV_Y = {{schema.common.defaultCamera.fovY}};
constexpr double COMMON_DEFAULT_CAMERA_NEAR = {{schema.common.defaultCamera.near}};
constexpr double COMMON_DEFAULT_CAMERA_FAR = {{schema.common.defaultCamera.far}};
constexpr double COMMON_EXP_APPROX_SCALE = {{schema.common.expApprox.scale}};
constexpr double COMMON_EXP_APPROX_BIAS = {{schema.common.expApprox.bias}};
constexpr double COMMON_EXP_APPROX_CLAMP = {{schema.common.expApprox.clamp}};

}
